/** @todo add all of the possible trait names instead of generic strings */

export interface Traits {
  base: 'Devie' | 'Maggie' | 'Johnny' | 'Swervie';
  arms: string;
  chest: string;
  color: string;
  background: string;
}
